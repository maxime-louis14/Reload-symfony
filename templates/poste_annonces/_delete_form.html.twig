<form method="post" action="{{ path('app_poste_annonces_delete', {'id': AnnoncesUser.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ AnnoncesUser.id) }}">
    <button class="btn">Effacer</button>
</form>
